<% if @selfy.errors.any? %>
  <ul>
  <% @selfy.errors.full_messages.each do |err| %>
    <li><%= err %></li>
  <% end %>
  </ul>
<% end %>

<%= form_for @selfy do |f| %>
  <%= f.text_area :body %><br>
  <%= f.submit %>
<% end %>